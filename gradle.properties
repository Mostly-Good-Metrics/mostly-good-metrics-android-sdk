# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Maven publishing properties.
# NOTE: the canonical version lives in mostlygoodmetrics/build.gradle.kts
# (`version = "…"`, bumped by release CI). VERSION_NAME is kept in sync here to
# avoid a misleading mismatch; it is not read by the publishing config.
GROUP=com.mostlygoodmetrics
VERSION_NAME=0.5.0
